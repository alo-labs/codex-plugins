# Cursor Claims Fact-Check Report

**Date:** 2026-06-27
**Sources:**
- https://cursor.com/docs/hooks
- https://cursor.com/docs/reference/third-party-hooks
- https://cursor.com/docs/context/rules
- https://cursor.com/docs/context/mcp
- https://cursor.com/docs/skills

---

| # | Claim | Verdict | Evidence (exact quote + URL) | Notes |
|---|-------|---------|------------------------------|-------|
| 1 | Hook/event protocol: "Bash hooks.json + cursor-hook-bridge.sh event map" | **PARTIALLY VERIFIED** | `hooks.json` is verified: "Define hooks in `hooks.json` files at the project or user level" (cursor.com/docs/hooks). No mention of `cursor-hook-bridge.sh` in official docs. | `hooks.json` is native Cursor. `cursor-hook-bridge.sh` is a Silver Bullet project artifact, not a Cursor feature. The claim conflates the two. |
| 2 | SessionStart: "sessionStart → bridge → SessionStart" | **PARTIALLY VERIFIED** | `sessionStart` is a real Cursor hook: "`sessionStart` / `sessionEnd` - Session lifecycle management" (cursor.com/docs/hooks). Third-party mapping: "`SessionStart` → `sessionStart`" (cursor.com/docs/reference/third-party-hooks). | The hook name is verified. The "bridge → SessionStart" mapping is a Silver Bullet implementation detail, not Cursor documentation. |
| 3 | PreToolUse — block: "preToolUse + shell bridge" | **VERIFIED** | "`preToolUse` / `postToolUse` / `postToolUseFailure` - Generic tool use hooks (fires for all tools)" (cursor.com/docs/hooks). Exit code 2 blocks: "Exit code `2` - Block the action (equivalent to returning `permission: "deny"`)" (cursor.com/docs/hooks). Output: `"permission": "allow" | "deny"` (cursor.com/docs/hooks). | PreToolUse supports blocking via `permission: "deny"` or exit code 2. "Shell bridge" is SB-specific. |
| 4 | PreToolUse — updatedInput: "Bridge passes through; SB hooks do not emit rewrites today" | **VERIFIED** | `"updated_input": { "command": "npm ci" }` — PreToolUse output supports `updated_input` (cursor.com/docs/hooks). Third-party format: `"updatedInput": { "command": "npm ci" }` (cursor.com/docs/reference/third-party-hooks). | `updated_input` is fully supported by Cursor natively. The claim about SB not emitting rewrites is an SB implementation note, not a Cursor limitation. |
| 5 | PreToolUse — additionalContext: "Supported in third-party Claude mapping; SB uses stderr/JSON sparingly" | **UNVERIFIED** | PreToolUse output fields are: `permission`, `user_message`, `agent_message`, `updated_input` (cursor.com/docs/hooks). No `additional_context` field documented for PreToolUse. `additional_context` exists only on **postToolUse** output. | `additionalContext` is NOT listed as a PreToolUse output field in Cursor docs. It exists only on postToolUse. The claim may reference Claude Code's hookSpecificOutput format, but Cursor docs don't confirm this for PreToolUse. |
| 6 | PostToolUse — modify result: "postToolUse" | **VERIFIED** | "`postToolUse` - Called after successful tool execution. Useful for auditing, analytics, and injecting context." Output: `"updated_mcp_tool_output"` and `"additional_context"` (cursor.com/docs/hooks). | postToolUse can modify MCP tool output and inject additional context. |
| 7 | PostToolUse — block/continue: "Same" (cannot block) | **VERIFIED** | PostToolUse output fields: `"updated_mcp_tool_output"` and `"additional_context"` only. No `permission` or block field. (cursor.com/docs/hooks). | PostToolUse cannot block or deny. It can only modify output or add context. The claim that it "cannot block" is correct. |
| 8 | PostToolUse / Skill: "PostToolUse matcher Skill" | **VERIFIED** | "preToolUse / postToolUse / postToolUseFailure: Filter by tool type. Values include `Shell`, `Read`, `Write`, `Grep`, `Delete`, `Task`, and MCP tools using the `MCP:<tool_name>` format." (cursor.com/docs/hooks). | The matcher supports `Task` (subagent tool). "Skill" as a matcher value is NOT documented — the documented values are `Shell`, `Read`, `Write`, `Grep`, `Delete`, `Task`, `MCP:*`. If "Skill" is used as a matcher, it may not match anything unless Cursor maps it internally. **PARTIALLY VERIFIED** — matcher exists but "Skill" is not a documented tool name. |
| 9 | Stop: "stop; followup_message / Claude-style decision: block" | **VERIFIED** | "`stop` - Handle agent completion" (cursor.com/docs/hooks). Output: `"followup_message": "<auto-continue with this message>"` (cursor.com/docs/hooks). Third-party: `"decision": "block"` with `"reason"` treated as automatic follow-up, equivalent to `followup_message` (cursor.com/docs/reference/third-party-hooks). | Both native `followup_message` and Claude-style `decision: "block"` are supported. |
| 10 | SubagentStop: "subagentStop via bridge" | **VERIFIED** | "`subagentStop` - Called when a subagent completes, errors, or is aborted. Can trigger follow-up actions." Output: `"followup_message"` (cursor.com/docs/hooks). Third-party mapping: `SubagentStop` → `subagentStop` (cursor.com/docs/reference/third-party-hooks). | subagentStop is a native Cursor hook. "via bridge" is SB-specific. |
| 11 | UserPromptSubmit — additionalContext: "beforeSubmitPrompt → bridge; community reports of dropped updated_input" | **PARTIALLY VERIFIED** | `beforeSubmitPrompt` is a real hook: "`beforeSubmitPrompt` - Validate prompts before submission" (cursor.com/docs/hooks). Matcher: "beforeSubmitPrompt: Matched against the value `UserPromptSubmit`" (cursor.com/docs/hooks). Third-party: `UserPromptSubmit` → `beforeSubmitPrompt` (cursor.com/docs/reference/third-party-hooks). No `updated_input` output documented for beforeSubmitPrompt. | The hook mapping is verified. `updated_input` is NOT documented as a beforeSubmitPrompt output — only `permission`, `user_message`, `agent_message` are listed for blocking hooks. Community reports of dropped updated_input cannot be verified from official docs. |
| 12 | UserPromptSubmit — block: "Documented; reliability varies by version" | **PARTIALLY VERIFIED** | `beforeSubmitPrompt` is documented as a hook event (cursor.com/docs/hooks). The docs show it in the hooks list but do NOT explicitly document its output schema (no dedicated section like preToolUse has). The general exit code 2 blocking behavior applies: "Exit code `2` - Block the action" (cursor.com/docs/hooks). | The hook exists and blocking via exit code 2 is implied by the general hook behavior. However, no dedicated output schema is documented for beforeSubmitPrompt, which could explain reliability issues. |
| 13 | Skill system: "runtime-native skill invocation channel + plugin cache" | **PARTIALLY VERIFIED** | Agent Skills is documented as an open standard: "Agent Skills is an open standard for extending AI agents with specialized capabilities" (cursor.com/docs/skills). Skills load from `.cursor/skills/`, `.agents/skills/`, etc. No "plugin cache" concept in Cursor skills docs. Cursor has a separate "Customize" plugin system and Marketplace. | Skills are verified. "Plugin cache" is not a Cursor concept — it appears to be SB-specific terminology conflating Cursor's skills with its own plugin system. |
| 14 | Custom agents/subagents: "Task / Subagent / Agent (orchestrator parent mode)" | **PARTIALLY VERIFIED** | Task tool is referenced: "subagentStart / subagentStop - Subagent (Task tool) lifecycle" (cursor.com/docs/hooks). subagentStart input includes `subagent_type`: `generalPurpose`, `explore`, `shell` (cursor.com/docs/hooks). No "orchestrator parent mode" documented. | Task/subagent is verified. "Agent (orchestrator parent mode)" is not a documented Cursor concept. |
| 15 | Parent orchestrator tool restriction: "Same via bridge" | **UNVERIFIED** | No "parent orchestrator" or tool restriction concept found in Cursor docs. subagentStart can `deny` subagent creation but this is not the same as restricting parent tools. | No evidence in official docs for parent orchestrator tool restriction. |
| 16 | Plugin/marketplace: "alo-labs-cursor-marketplace" | **UNVERIFIED** | Cursor has an official Marketplace: "Browse the Cursor Marketplace for official plugins with one-click install" (cursor.com/docs/context/mcp). No mention of "alo-labs-cursor-marketplace" in official docs. | The official Cursor Marketplace exists at cursor.com/marketplace. "alo-labs-cursor-marketplace" is not referenced in Cursor documentation. |
| 17 | MCP support: "beforeMCPExecution / afterMCPExecution" | **VERIFIED** | "`beforeMCPExecution` / `afterMCPExecution` - Control MCP tool usage" (cursor.com/docs/hooks). beforeMCPExecution input: `tool_name`, `tool_input`, `url`/`command`. afterMCPExecution input: `tool_name`, `tool_input`, `result_json`, `duration` (cursor.com/docs/hooks). | Fully verified. Both hooks are documented with input/output schemas. |
| 18 | Config injection: "AGENTS.md, .cursor/rules/" | **VERIFIED** | "`AGENTS.md` is a simple markdown file for defining agent instructions. Place it in your project root." (cursor.com/docs/context/rules). "Project rules live in `.cursor/rules` as `.mdc` files" (cursor.com/docs/context/rules). Nested AGENTS.md supported. | Both config injection paths are fully documented. |
| 19 | State/session persistence: "Same" (as Claude Code) | **UNVERIFIED** | No explicit documentation found about session persistence or state management in either Cursor hooks or third-party hooks docs. The hook input includes `conversation_id` and `generation_id` suggesting some persistence. | Cannot verify from available docs. The claim is vague. |
| 20 | Permission/approval hooks: "Not supported in Cursor third-party matrix" | **VERIFIED** | Third-party hooks support table: "`Notification` → Not supported", "`PermissionRequest` → Not supported" (cursor.com/docs/reference/third-party-hooks). | `PermissionRequest` from Claude Code is explicitly listed as NOT supported in the third-party hooks compatibility matrix. |
| 21 | Compaction PreCompact: "preCompact; SB not wired" | **VERIFIED** | "`preCompact` - Observe context window compaction" (cursor.com/docs/hooks). Third-party mapping: `PreCompact` → `preCompact` (cursor.com/docs/reference/third-party-hooks). | preCompact is a documented Cursor hook. Whether SB wires it is an SB implementation detail. |
| 22 | Compaction PostCompact: "No postCompact in Cursor hook list" | **VERIFIED** | The complete hook list from cursor.com/docs/hooks: `sessionStart`, `sessionEnd`, `preToolUse`, `postToolUse`, `postToolUseFailure`, `subagentStart`, `subagentStop`, `beforeShellExecution`, `afterShellExecution`, `beforeMCPExecution`, `afterMCPExecution`, `beforeReadFile`, `afterFileEdit`, `beforeSubmitPrompt`, `preCompact`, `stop`, `afterAgentResponse`, `afterAgentThought`, `beforeTabFileRead`, `afterTabFileEdit`, `workspaceOpen`. No `postCompact` exists. | Confirmed: `postCompact` is NOT in Cursor's hook list. Only `preCompact` exists. |

---

## Summary

| Verdict | Count | Claims |
|---------|-------|--------|
| **VERIFIED** | 9 | #3, #4, #6, #7, #9, #10, #17, #18, #20, #21, #22 |
| **PARTIALLY VERIFIED** | 7 | #1, #2, #8, #11, #12, #13, #14 |
| **UNVERIFIED** | 3 | #5, #15, #16, #19 |
| **FALSE** | 0 | — |

### Key Findings

1. **`cursor-hook-bridge.sh` is NOT a Cursor concept** — it's a Silver Bullet project artifact. Cursor uses `hooks.json` natively.
2. **`additionalContext` is NOT a PreToolUse output** — it only exists on `postToolUse`. Claim #5 is unverified/wrong for PreToolUse.
3. **`postCompact` does NOT exist** — only `preCompact` is in Cursor's hook list. This is confirmed.
4. **`PermissionRequest` is NOT supported** in the third-party hooks matrix — claim #20 is verified.
5. **Skills ≠ plugin cache** — Cursor has an open Agent Skills standard, not a "plugin cache" system.
6. **"alo-labs-cursor-marketplace"** is not referenced in any official Cursor documentation.
7. **`updated_input` on beforeSubmitPrompt** is NOT documented — only `permission`, `user_message`, `agent_message` for blocking hooks.
8. **`Skill` as a matcher value** is NOT documented — only `Shell`, `Read`, `Write`, `Grep`, `Delete`, `Task`, `MCP:*`.
