# OpenCode Claims Verification Report

Generated: 2026-06-27
Sources: https://opencode.ai/docs/plugins, https://opencode.ai/docs/config, https://opencode.ai/docs/agents, https://opencode.ai/docs/skills, https://opencode.ai/docs/mcp-servers, https://opencode.ai/docs/permissions, https://opencode.ai/docs/tools

---

| # | Claim | Verdict | Evidence | Notes |
|---|-------|---------|----------|-------|
| 1 | TypeScript plugins; 30+ events including tool.execute.before/after, session.*, permission.* | **PARTIALLY VERIFIED** | TypeScript plugins confirmed: `"import type { Plugin } from "@opencode-ai/plugin"` ([plugins](https://opencode.ai/docs/plugins)). Events listed: command.executed, file.edited, file.watcher.updated, installation.updated, lsp.client.diagnostics, lsp.updated, message.part.removed, message.part.updated, message.removed, message.updated, permission.asked, permission.replied, server.connected, session.created, session.compacted, session.deleted, session.diff, session.error, session.idle, session.status, session.updated, todo.updated, shell.env, tool.execute.after, tool.execute.before, tui.prompt.append, tui.command.execute, tui.toast.show = **28 events** + `experimental.session.compacting` = 29. tool.execute.before/after confirmed. session.* (8 events) confirmed. permission.* (2 events) confirmed. | Event count is ~28-29, not "30+". Close but overstated. |
| 2 | SessionStart: session.created; no startup/resume/clearcontext compaction source discriminator but session.compacted exists | **VERIFIED** | `session.created` listed under Session Events ([plugins](https://opencode.ai/docs/plugins)). `session.compacted` also listed. No `source` field documented on session.created in OpenCode docs. (Note: `source` discriminator with startup/resume/clearcontext compaction is a **Codex** feature, not OpenCode.) | Correct — OpenCode has no source discriminator on session.created. |
| 3 | PreToolUse — block: tool.execute.before + permission config with deny glob patterns | **VERIFIED** | `tool.execute.before` event exists ([plugins](https://opencode.ai/docs/plugins)). Permission config supports `"deny"` with glob patterns: `"rm -rf *": "deny"` ([agents](https://opencode.ai/docs/agents)). Throwing an error in tool.execute.before blocks the tool: `throw new Error("Do not read .env files")` ([plugins](https://opencode.ai/docs/plugins)). | Blocking via throw + permission deny both confirmed. |
| 4 | PreToolUse — updatedInput: Plugin can modify tool args via tool.execute.before handler | **VERIFIED** | Shescape example: `"tool.execute.before": async (input, output) => { if (input.tool === "bash") { output.args.command = escape(output.args.command) } }` ([plugins](https://opencode.ai/docs/plugins)). | Direct mutation of `output.args` confirmed. |
| 5 | PreToolUse — additionalContext: Plugin can inject context via event handler return | **PARTIALLY VERIFIED** | Context injection confirmed for **compaction hooks**: `output.context.push(...)` in `experimental.session.compacting` ([plugins](https://opencode.ai/docs/plugins)). However, no documented `output.context` or additionalContext mechanism for `tool.execute.before` specifically. | Context injection exists for compaction only; not documented for tool.execute.before. |
| 6 | PostToolUse — modify result: tool.execute.after fires but documented as observe-only | **UNVERIFIED** | `tool.execute.after` is listed as an event ([plugins](https://opencode.ai/docs/plugins)). No documentation found explicitly stating it is "observe-only" or prohibiting result modification. No examples show modifying results via tool.execute.after. | Cannot confirm "observe-only" claim — documentation is silent on this. |
| 7 | PostToolUse — block/continue: tool.execute.after fires; no documented block/continue contract | **VERIFIED** | `tool.execute.after` fires ([plugins](https://opencode.ai/docs/plugins)). No documentation found for any block/continue mechanism on tool.execute.after. | Correct — no block/continue contract documented. |
| 8 | PostToolUse / Skill: skill tool exists; tool.execute.after can match skill invocations | **VERIFIED** | Native `skill` tool confirmed: "Skills are loaded on-demand via the native `skill` tool" ([skills](https://opencode.ai/docs/skills)). `tool.execute.after` fires for all tools and can match on `input.tool === "skill"`. | Technically correct — skill tool exists and tool.execute.after can filter by tool name. |
| 9 | Stop: session.idle / session.status events; no documented decision: block contract | **VERIFIED** | `session.idle` and `session.status` listed under Session Events ([plugins](https://opencode.ai/docs/plugins)). No documentation found for any decision:block contract on these events. | Correct — no such contract documented. |
| 10 | SubagentStop: No dedicated SubagentStop; session.deleted / session.status fire for child sessions | **VERIFIED** | No `SubagentStop` event in the events list ([plugins](https://opencode.ai/docs/plugins)). `session.deleted` and `session.status` are documented session events. Subagents create child sessions with navigation support: `session_child_first`, `session_child_cycle`, `session_parent` ([agents](https://opencode.ai/docs/agents)). | Correct — no dedicated SubagentStop; session events fire for all sessions including children. |
| 11 | UserPromptSubmit — additionalContext: tui.prompt.append event; can modify prompt content | **PARTIALLY VERIFIED** | `tui.prompt.append` listed under TUI Events ([plugins](https://opencode.ai/docs/plugins)). Name suggests prompt modification capability, but **no documentation or examples** show how to modify prompt content via this event. | Event exists but modification capability is not explicitly documented. |
| 12 | UserPromptSubmit — block: permission.asked / permission.replied events; no prompt-block contract | **VERIFIED** | `permission.asked` and `permission.replied` listed under Permission Events ([plugins](https://opencode.ai/docs/plugins)). No documentation found for a prompt-block contract on user prompt submission. | Correct — no prompt-block contract documented. |
| 13 | Skill system: SKILL.md format; discovers .opencode/skills/, .codex/skills/, .agents/skills/ + global paths; native skill tool | **VERIFIED** | "Create a `skills/*/SKILL.md` in `.opencode/` and any matching `.codex/skills/*/SKILL.md` or `.agents/skills/*/SKILL.md`" plus global: `~/.config/opencode/skills/*/SKILL.md`, `$HOME/.codex/skills/*/SKILL.md`, `~/.agents/skills/*/SKILL.md` ([skills](https://opencode.ai/docs/skills)). Native `skill` tool confirmed. | All paths and SKILL.md format confirmed exactly. |
| 14 | Custom agents/subagents: Primary + subagent architecture; built-in General/Explore/Scout; custom via JSON/Markdown; task permission control | **VERIFIED** | "Two types of agents: primary agents and subagents" ([agents](https://opencode.ai/docs/agents)). Built-in subagents: **General**, **Explore**, **Scout**. Built-in primary agents: **Build**, **Plan**. Custom via JSON config or Markdown files in `~/.config/opencode/agents/` or `.opencode/agents/`. Task permissions with glob patterns: `"*": "deny", "orchestrator-*": "allow"`. | All aspects confirmed. Note: built-in primary agents are Build+Plan (not mentioned in claim). |
| 15 | Parent orchestrator tool restriction: permission config with deny patterns on edit/bash/task | **VERIFIED** | Permission config supports deny patterns: `"edit": "deny"`, `"bash": "deny"`, `"task": { "*": "deny" }` ([agents](https://opencode.ai/docs/agents)). Example: plan agent has `edit: deny, bash: deny`. | Confirmed with glob pattern support. |
| 16 | Plugin/marketplace: .opencode/plugins/ + ~/.config/opencode/plugins/; no SB marketplace | **VERIFIED** | ".opencode/plugins/ - Project-level plugins, ~/.config/opencode/plugins/ - Global plugins" ([plugins](https://opencode.ai/docs/plugins)). Ecosystem page lists community projects but no marketplace ([ecosystem](https://opencode.ai/docs/ecosystem)). | Plugin paths confirmed. No marketplace found. |
| 17 | MCP support: First-class; local (stdio) + remote (URL); OAuth support; org-level defaults via .well-known/opencode | **VERIFIED** | "OpenCode supports both local and remote servers" ([mcp-servers](https://opencode.ai/docs/mcp-servers)). Local: `"type": "local", "command": [...]`. Remote: `"type": "remote", "url": "..."`. OAuth: "OpenCode automatically handles OAuth authentication" with Dynamic Client Registration (RFC 7591). `.well-known/opencode` for org defaults ([config](https://opencode.ai/docs/config)). | All aspects confirmed. |
| 18 | Config injection: AGENTS.md, CLAUDE.md, custom instruction paths + globs, remote URLs | **PARTIALLY VERIFIED** | `instructions` config option: `"instructions": ["CONTRIBUTING.md", "docs/guidelines.md", ".cursor/rules/*.md"]` — takes array of paths and glob patterns ([config](https://opencode.ai/docs/config)). However, **AGENTS.md and CLAUDE.md are not OpenCode-specific concepts** — they are Codex/Claude Code conventions. No evidence of remote URL support for instructions. | Custom paths + globs confirmed. AGENTS.md/CLAUDE.md references are misleading — those are competitor conventions, not OpenCode features. |
| 19 | State/session persistence: session.created + session.compacted plugin hooks; state in ~/.config/opencode/ | **VERIFIED** | `session.created` and `session.compacted` events confirmed ([plugins](https://opencode.ai/docs/plugins)). Config and state stored in `~/.config/opencode/` ([config](https://opencode.ai/docs/config)). | Confirmed. |
| 20 | Permission/approval hooks: permission.asked / permission.replied events + per-tool allow/ask/deny with glob patterns | **VERIFIED** | `permission.asked` and `permission.replied` events ([plugins](https://opencode.ai/docs/plugins)). Per-tool: `"allow"`, `"ask"`, `"deny"` with glob patterns. Example: `"bash": { "*": "ask", "rm -rf *": "deny" }` ([config](https://opencode.ai/docs/config)). Wildcard patterns for MCP tools: `"mymcp_*": "deny"`. | All aspects confirmed. |
| 21 | Compaction PreCompact: experimental.session.compacting; inject context or replace prompt | **VERIFIED** | `"experimental.session.compacting": async (input, output) => { output.context.push(...) }` for injecting context. `output.prompt = ...` for replacing the entire compaction prompt ([plugins](https://opencode.ai/docs/plugins)). | Both inject and replace confirmed. |
| 22 | Compaction PostCompact: session.compacted event fires after compaction | **VERIFIED** | `session.compacted` listed under Session Events ([plugins](https://opencode.ai/docs/plugins)). | Confirmed. |

---

## Summary

| Verdict | Count | Claims |
|---------|-------|--------|
| **VERIFIED** | 16 | #2, #3, #4, #7, #8, #9, #10, #12, #13, #14, #15, #16, #17, #19, #20, #21, #22 |
| **PARTIALLY VERIFIED** | 4 | #1 (event count ~28-29 not 30+), #5 (context injection only for compaction), #11 (event exists but modification undocumented), #18 (AGENTS.md/CLAUDE.md are not OpenCode concepts) |
| **UNVERIFIED** | 1 | #6 (observe-only claim not found in docs) |
| **FALSE** | 0 | — |

### Key Findings

1. **Event count overstated**: OpenCode has ~28-29 events, not "30+" as claimed.
2. **AGENTS.md/CLAUDE.md misattributed**: These are Codex/Claude Code conventions, not OpenCode features. OpenCode has its own `instructions` config with paths/globs.
3. **tool.execute.after behavior undocumented**: The claim that it's "observe-only" cannot be verified — docs are silent on whether it can modify results.
4. **Context injection scope narrower than claimed**: `output.context.push()` exists only for compaction hooks, not for general tool.execute.before use.
5. **tui.prompt.append modification capability**: Event exists but no documentation shows how to use it for prompt modification.
