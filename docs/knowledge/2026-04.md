---
project: Silver Bullet
period: 2026-04
type: knowledge
---

# Project Knowledge — 2026-04

## Architecture Patterns

2026-04-05 — Enforcement is split across two lists in `.silver-bullet.json`: `all_tracked` (discovery surface — hooks record the invocation) and `required_deploy` (hard gate — `completion-audit.sh` blocks commit/push/deploy if missing). Adding a skill to only `all_tracked` gives observability but not enforcement. Both lists must be mirrored in `templates/silver-bullet.config.json.default` so new projects inherit all enforcement rules.

2026-04-05 — `hooks/dev-cycle-check.sh` has a `finalization_skills` variable (line 205) that must stay in sync with `required_deploy`. Currently hardcoded — if a skill is added to `required_deploy` without updating `finalization_skills`, phase-skip detection silently misses the out-of-order case.

## Known Gotchas

2026-04-05 — `accessibility-review` and `incident-response` must NOT be added to `required_deploy`. Both are conditional (UI-work path and DevOps incident path respectively). Adding them to `required_deploy` would cause `completion-audit.sh` to block commits on every non-UI and non-incident workflow run.

2026-04-05 — `templates/silver-bullet.config.json.default` is easy to forget when updating `.silver-bullet.json`. The two files must stay in exact parity on `required_deploy` and `all_tracked`.

## Key Decisions

2026-04-05 — Chose v0.8.0 (semver minor) for adding skills to `required_deploy` because it blocks existing users' commits until they invoke the newly required skills. Breaking change documented in release notes.

2026-04-05 — `tech-debt` replaces the pre-existing inline prose note in FINALIZATION step 14. The prose note was informal and not enforced by any hook. The skill invocation is now required and enforcement-tracked.

## Recurring Patterns

2026-04-25 — When promoting a new skill to required/tracked status in Silver Bullet, 4 files always need updating together: (1) all_tracked array in .silver-bullet.json, (2) required_deploy array in .silver-bullet.json, (3) templates/silver-bullet.config.json.default mirror, and (4) hooks/dev-cycle-check.sh finalization_skills variable. Missing any one causes enforcement drift or a hook/config mismatch that silently fails to gate deploys.

2026-04-05 — Every skill promoted to explicit enforcement requires 4 coordinated changes: (1) workflow doc REQUIRED marker, (2) template mirror update, (3) `.silver-bullet.json` `all_tracked` entry, (4) `required_deploy` entry if unconditional. Missing any one produces an inconsistency.

## Open Questions

2026-04-05 — Should `finalization_skills` in `dev-cycle-check.sh` be derived at runtime from `.silver-bullet.json` `required_deploy` rather than hardcoded?
[RESOLVED 2026-04-05]: Deferred to a future refactor — tracked as tech debt (score 18). Current manual sync is acceptable while `required_deploy` changes are infrequent.

## Infrastructure Config

2026-04-24 — Forge uses MiniMax-M2.7 via `https://api.minimax.io/anthropic/v1/messages`. Model configuration lives in a TOML file under `~/.claude/plugins/cache/forge*/config/`. Settings include `model`, `max_tokens`, and provider endpoint. Changes take effect on next forge invocation — no restart needed.

2026-04-24 — The `max_tokens` setting was increased from a lower default to `196608` (192K tokens). This is near MiniMax-M2.7's maximum response length and allows forge to generate much longer outputs without truncation.

2026-04-24 — `forge config list` emits ANSI color codes that break text processing. When scripting config validation, strip codes first:
```bash
forge config list 2>/dev/null | sed 's/\x1b\[[0-9;]*m//g' | awk 'NR==27 {print $3}'
```
Verify with `forge config get max_tokens` which returns the raw value without color codes.

2026-04-24 — Test script at `tests/forge-test-app/test-minimax-config.sh` validates the active config. Run it post-change to confirm `max_tokens = 196608` is loaded.
