# OMP (oh-my-pi) Fact-Check Report

**Date:** 2026-06-27
**Sources:** GitHub README (raw), source code (`packages/agent/src/agent-loop.ts`, `packages/agent/src/agent.ts`), `packages/swarm-extension/`, blog posts

---

| # | Claim | Verdict | Evidence | Notes |
|---|-------|---------|----------|-------|
| 1 | Hook/event protocol: "TypeScript extensions (pi.on); separate legacy hooks API" | **PARTIALLY VERIFIED** | "An extension is a TypeScript module. Same tool API, same slash-command registry, same hotkey table, same TUI primitives the built-ins use." — [README](https://github.com/can1357/oh-my-pi/blob/main/README.md). The API uses `beforeToolCall`, `afterToolCall`, `transformAssistantMessage`, `onTurnEnd`, `onRunEnd`, and `subscribe()` on the Agent class — [agent.ts:source](https://github.com/can1357/oh-my-pi/blob/main/packages/agent/src/agent.ts). | Extensions are TypeScript modules — **correct**. However, `pi.on` is **not** the API name; the actual hooks are `beforeToolCall`, `afterToolCall`, etc. on `AgentLoopConfig`. No evidence of a "separate legacy hooks API". |
| 2 | SessionStart: "session_start; no startup/resume/clearcontext compaction source discriminator" | **UNVERIFIED** | No `session_start` event or hook found in source. The agent-loop emits `agent_start` and `turn_start` events — [agent-loop.ts](https://github.com/can1357/oh-my-pi/blob/main/packages/agent/src/agent-loop.ts). No evidence of startup/resume/clearcontext compaction discriminator. | The event is `agent_start`, not `session_start`. No discriminator concept found in public docs or source. |
| 3 | PreToolUse — block: "tool_call → { block, reason } only" | **VERIFIED** | `if (beforeResult?.block) { throw new ToolCallBlockedError(beforeResult.reason); }` — [agent-loop.ts](https://github.com/can1357/oh-my-pi/blob/main/packages/agent/src/agent-loop.ts). The `beforeToolCall` hook receives `{ assistantMessage, toolCall, args, context }` and can return `{ block: boolean, reason: string }`. | Exact match. The hook blocks via `ToolCallBlockedError`. |
| 4 | PreToolUse — updatedInput: "No input-rewrite contract on tool_call" | **FALSE** | `const executionArgs = transformToolCallArguments ? transformToolCallArguments(effectiveArgs, toolCall.name) : effectiveArgs;` — [agent-loop.ts](https://github.com/can1357/oh-my-pi/blob/main/packages/agent/src/agent-loop.ts). | omp **does** have an input-rewrite contract via `transformToolCallArguments` in `AgentLoopConfig`. The claim is factually wrong. |
| 5 | PreToolUse — additionalContext: "Not on tool_call" | **UNVERIFIED** | The `beforeToolCall` hook receives `{ assistantMessage, toolCall, args, context }`. No explicit `additionalContext` return field found. The hook can only return `{ block, reason }`. | No `additionalContext` return contract found on `beforeToolCall`. However, the hook receives the full `context` (AgentContext) as input, which could serve a similar purpose. |
| 6 | PostToolUse — modify result: "tool_result patches content / details / isError only" | **VERIFIED** | `const coerced = coerceToolResult({ content: after.content ?? result.content, details: after.details ?? result.details, isError: after.isError ?? result.isError, useless: after.useless ?? result.useless });` — [agent-loop.ts](https://github.com/can1357/oh-my-pi/blob/main/packages/agent/src/agent-loop.ts). | Exact match. The `afterToolCall` hook can return modified `content`, `details`, `isError`, and `useless`. |
| 7 | PostToolUse — block/continue: "No decision: block continuation contract" | **UNVERIFIED** | The `afterToolCall` hook can only modify the result (content/details/isError). No evidence of a block/continue decision contract on post-tool-use. | The hook modifies results but cannot block continuation. The claim appears directionally correct but the phrasing is unclear. |
| 8 | PostToolUse / Skill: "Skills load via skill:// / /skill:; no hook-fired skill invocation event" | **PARTIALLY VERIFIED** | "Twelve internal schemes — `pr://`, `issue://`, `agent://`, `skill://`, `rule://`, and the rest — resolve transparently inside every FS-shaped tool the agent already calls." — [README](https://github.com/can1357/oh-my-pi/blob/main/README.md). | Skills are accessed via `skill://` URL scheme through the `read` tool — **correct**. No evidence of a hook-fired skill invocation event. |
| 9 | Stop: "session_stop (OMP v16+, PR #2845): main-session only; { continue, additionalContext } or { decision: block, reason }; 8-cap" | **FALSE** | No `session_stop` hook found in source. The agent-loop emits `agent_end` event — [agent-loop.ts](https://github.com/can1357/oh-my-pi/blob/main/packages/agent/src/agent-loop.ts). No PR #2845 reference found. | The event is `agent_end`, not `session_stop`. No `{ continue, additionalContext }` or `{ decision: block, reason }` return contract found. The PR number and version reference cannot be verified. |
| 10 | SubagentStop: "No subagent_stop; subagents use generic agent_end (notification-only)" | **UNVERIFIED** | The swarm-extension manages subagents with lifecycle orchestration — [swarm-extension](https://github.com/can1357/oh-my-pi/tree/main/packages/swarm-extension). The agent-loop emits `agent_end` for each agent run. | No `subagent_stop` event found. Subagents appear to use `agent_end` but the "notification-only" characterization cannot be verified from public source. |
| 11 | UserPromptSubmit — additionalContext: "input / before_agent_start; not a prompt-submit context contract" | **UNVERIFIED** | No `UserPromptSubmit` hook found in source. The agent-loop has `agent_start` and `turn_start` events. | No equivalent to Claude Code's `UserPromptSubmit` hook found. The claim about "input / before_agent_start" cannot be verified. |
| 12 | UserPromptSubmit — block: "No equivalent block contract" | **UNVERIFIED** | No prompt-blocking mechanism found in the public source code or documentation. | No evidence of prompt-blocking found, but absence of evidence is not evidence of absence. |
| 13 | Skill system: "~/.omp/agent/skills/, .omp/skills/; also discovers Claude/Codex skill dirs; model-triggered load, not hook-recorded" | **PARTIALLY VERIFIED** | "On first run omp inherits whatever is already on disk: rules, skills, and MCP servers from `.claude`, `.cursor`, `.windsurf`, `.gemini`, `.codex`, `.cline`, `.github/copilot`, and `.vscode`." — [README](https://github.com/can1357/oh-my-pi/blob/main/README.md). Skills accessed via `skill://` scheme. | Discovery from Claude/Codex dirs — **verified**. However, `~/.omp/agent/skills/` and `.omp/skills/` paths are **not documented** in public sources. Config is in `~/.omp/config.json` or `.omp/config.json` per swarm-extension docs. "Model-triggered load" is plausible given `skill://` URL scheme but not explicitly documented. |
| 14 | Custom agents/subagents: "task tool (child omp processes + IRC bus)" | **PARTIALLY VERIFIED** | "task fans out into isolated worktrees, each worker runs its own tool surface, and the final yield is a schema-validated object the parent reads directly." — [README](https://github.com/can1357/oh-my-pi/blob/main/README.md). "the constraints block requiring an IRC DM between peers" — README screenshot caption. | `task` tool — **verified**. IRC bus is mentioned in a screenshot caption as a constraint mechanism between subagents, but "child omp processes" is not explicitly confirmed (worktrees could be in-process). |
| 15 | Parent orchestrator tool restriction: "Requires custom extension on tool_call; no SB pack shipped" | **UNVERIFIED** | No evidence found in public docs or source about parent orchestrator tool restrictions or "SB pack". | Cannot verify. The claim references Silver Bullet concepts that may not apply to omp. |
| 16 | Plugin/marketplace: "Extension paths in ~/.omp/agent/config.yml; no SB marketplace" | **PARTIALLY VERIFIED** | "Register the extension in your config (`~/.omp/config.json` or `.omp/config.json`)" — [swarm-extension](https://github.com/can1357/oh-my-pi/tree/main/packages/swarm-extension). "Ask omp to write the piece you're missing, then `/reload-plugins`. Keep it local, ship it in a `marketplace`, or publish it to npm." — [README](https://github.com/can1357/oh-my-pi/blob/main/README.md). | Extensions are in `config.json`, **not** `config.yml` as claimed. A marketplace concept exists. "No SB marketplace" — correct, omp has its own marketplace/npm distribution. |
| 17 | MCP support: "In-process MCP tools" | **PARTIALLY VERIFIED** | "On first run omp inherits whatever is already on disk: rules, skills, and MCP servers from `.claude`, `.cursor`, `.windsurf`, `.gemini`, `.codex`, `.cline`, `.github/copilot`, and `.vscode`." — [README](https://github.com/can1357/oh-my-pi/blob/main/README.md). MCP is listed as a topic on the repo. | MCP support exists — **verified**. "In-process" cannot be confirmed from public docs; MCP servers could be out-of-process. |
| 18 | Config injection: "Context files + config.yml; no silver-bullet.md hook merge" | **PARTIALLY VERIFIED** | Config is in `config.json` (not `config.yml`). "Time-traveling stream rules" inject rules mid-stream — [README](https://github.com/can1357/oh-my-pi/blob/main/README.md). | Config injection exists but via `config.json` and stream rules, **not** `config.yml`. "No silver-bullet.md hook merge" — correct, omp doesn't use silver-bullet.md. |
| 19 | State/session persistence: "session_start + appendEntry; different state root (~/.omp/agent/)" | **UNVERIFIED** | The agent uses `AgentContext` with messages array. The append-only-context module exists — [agent/src/](https://github.com/can1357/oh-my-pi/tree/main/packages/agent/src). Config references `~/.omp/agent/models.yml`. | `~/.omp/agent/` is used for models config — **partially verified**. `session_start` and `appendEntry` are not the actual API names. The append-only-context module exists but its API names differ. |
| 20 | Permission/approval hooks: "tool_approval_requested / resolved (observe only; no handler decision)" | **UNVERIFIED** | ACP uses `session/request_permission` for editor integration — [README](https://github.com/can1357/oh-my-pi/blob/main/README.md). "Destructive tools pause for a permission prompt you can answer once and forget." | Permission prompts exist in ACP context — **verified**. But `tool_approval_requested` / `tool_approval_resolved` are not the actual event names. "Observe only; no handler decision" cannot be verified. |
| 21 | Compaction PreCompact: "session_before_compact / session.compacting (cancel / inject summary)" | **UNVERIFIED** | A compaction module exists — [agent/srccontext compactionion/](https://github.com/can1357/oh-my-pi/tree/main/packages/agent/srccontext compactionion). "Injections survive compaction" — README. Snapcompact package handles compaction. | Compaction exists but `session_before_compact` / `session.compacting` are not documented event names. The claim uses Claude Code terminology, not omp terminology. |
| 22 | Compaction PostCompact: "session_compact (notification-only; handler return ignored)" | **UNVERIFIED** | No `session_compact` event found. The snapcompact package handles compaction — [snapcompact](https://github.com/can1357/oh-my-pi/tree/main/packages/snapcompact). | No evidence of `session_compact` hook. The claim uses Claude Code terminology. |

---

## Summary

| Verdict | Count | Claims |
|---------|-------|--------|
| **VERIFIED** | 2 | #3 (PreToolUse block), #6 (PostToolUse modify result) |
| **PARTIALLY VERIFIED** | 7 | #1 (extensions are TS, but API name wrong), #8 (skill:// correct), #13 (discovery dirs correct, skill paths wrong), #14 (task tool correct, IRC bus partial), #16 (config.json not config.yml), #17 (MCP exists, in-process unconfirmed), #18 (config injection exists, not config.yml) |
| **UNVERIFIED** | 10 | #2, #5, #7, #10, #11, #12, #15, #19, #20, #21, #22 |
| **FALSE** | 2 | #4 (transformToolCallArguments exists), #9 (no session_stop hook) |

## Key Findings

1. **The comparison document frequently uses Claude Code terminology** (e.g., `session_start`, `session_stop`, `session_before_compact`, `tool_approval_requested`) rather than omp's actual API names (`agent_start`, `agent_end`, `beforeToolCall`, `afterToolCall`, `onTurnEnd`).

2. **omp's actual hook API** is: `beforeToolCall` (can block + has `transformToolCallArguments`), `afterToolCall` (can modify result), `transformAssistantMessage`, `onTurnEnd`, `onRunEnd`, and `subscribe()` for event streaming.

3. **Claim #4 is factually wrong**: omp has `transformToolCallArguments` which rewrites tool call input before execution.

4. **Claim #9 is factually wrong**: There is no `session_stop` hook; the event is `agent_end`.

5. **Config paths are wrong**: omp uses `config.json` (not `config.yml`) and `models.yml`, located at `~/.omp/` or `.omp/` in the project root.

6. **Skill paths are unverified**: `~/.omp/agent/skills/` and `.omp/skills/` are not documented in public sources.
